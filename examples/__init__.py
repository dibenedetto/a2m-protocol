"""Runnable arguments, rather than runnable tests.

	cross_framework.py  LangChain and Agno over one A2M store, reading each
	                    other's records.

Needs the optional framework dependencies. Nothing else in the repository
imports this package, so a checkout without them still runs every test, every
conformance target and both demos.

	python -m examples.cross_framework
"""
